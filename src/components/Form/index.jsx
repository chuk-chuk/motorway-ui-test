import React, {  useState } from "react";
import "./styles.css";

const Form = () => {
  const [showForm, setShowForm] = useState(false);

  return (
    <div>Form goes here</div>
  );
}

export default Form;
